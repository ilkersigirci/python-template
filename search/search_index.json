{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Home","text":"<p>This site contains the project documentation for the <code>python_template</code> project.</p>"},{"location":"#table-of-contents","title":"Table Of Contents","text":"<p>The documentation follows the best practice for project documentation as described by Daniele Procida in the Di\u00e1taxis documentation framework and consists of four separate parts:</p> <ol> <li>Tutorials</li> <li>How-To Guides</li> <li>Reference</li> <li>Explanation</li> </ol> <p>Quickly find what you're looking for depending on your use case by looking at the different pages.</p>"},{"location":"#mkdocs","title":"MkDocs","text":"<ul> <li>For full documentation visit mkdocs.org.</li> </ul> <pre><code># Create a new project.\nmkdocs new [dir-name]\n\n# Start the live-reloading docs server.\nmkdocs serve\n\n# Build the documentation site.\nmkdocs build\n\n# Print help message and exit.\nmkdocs -h\n</code></pre>"},{"location":"explanation/","title":"Explanation","text":"<p>This part of the project documentation focuses on an understanding-oriented approach. You'll get a chance to read about the background of the project, as well as reasoning about how it was implemented.</p> <p>Note: Expand this section by considering the following points:</p> <ul> <li>Give context and background on your library</li> <li>Explain why you created it</li> <li>Provide multiple examples and approaches of how     to work with it</li> <li>Help the reader make connections</li> <li>Avoid writing instructions or technical descriptions     here</li> </ul>"},{"location":"how-to-guides/","title":"How to guides","text":"<p>This part of the project documentation focuses on a problem-oriented approach. You'll tackle common tasks that you might have, with the help of the code provided in this project.</p>"},{"location":"reference/","title":"Reference","text":"<p>This part of the project documentation focuses on an information-oriented approach. Use it as a reference for the technical implementation of the <code>python_template</code> project code.</p> <p>Python template package.</p>"},{"location":"reference/#python_template.loggers","title":"loggers","text":"<p>Custom loggers.</p>"},{"location":"reference/#python_template.loggers.configs","title":"configs","text":"<p>Loggers configs.</p>"},{"location":"reference/#python_template.loggers.configs.advanced","title":"advanced","text":"<p>Advanced logging configuration.</p>"},{"location":"reference/#python_template.loggers.configs.default","title":"default","text":"<p>Default logging configuration.</p>"},{"location":"reference/#python_template.loggers.configs.uvicorn","title":"uvicorn","text":"<p>Uvicorn logging configuration.</p>"},{"location":"reference/#python_template.loggers.filters","title":"filters","text":"<p>Filters for logging.</p>"},{"location":"reference/#python_template.loggers.filters.CwdFilter","title":"CwdFilter","text":"<p>               Bases: <code>Filter</code></p> <p>Filter that removes cwd from pathname.</p>"},{"location":"reference/#python_template.loggers.filters.CwdFilter.filter","title":"filter","text":"<pre><code>filter(record)\n</code></pre> <p>Filter function.</p> Source code in <code>src/python_template/loggers/filters.py</code> <pre><code>def filter(self, record: logging.LogRecord) -&gt; bool:\n    \"\"\"Filter function.\"\"\"\n    record.pathname = record.pathname.replace(str(Path.cwd()), \"\")\n\n    return True\n</code></pre>"},{"location":"reference/#python_template.loggers.filters.IgnoreThreadsFilter","title":"IgnoreThreadsFilter","text":"<pre><code>IgnoreThreadsFilter()\n</code></pre> <p>               Bases: <code>Filter</code></p> <p>Only accepts log records that originated from the main thread.</p> <p>Attributes:</p> Name Type Description <code>_main_thread_id</code> <p>Id of the main thread.</p> Source code in <code>src/python_template/loggers/filters.py</code> <pre><code>def __init__(self) -&gt; None:\n    self._main_thread_id = threading.main_thread().ident\n</code></pre>"},{"location":"reference/#python_template.loggers.filters.IgnoreThreadsFilter.filter","title":"filter","text":"<pre><code>filter(record)\n</code></pre> <p>Filter function.</p> Source code in <code>src/python_template/loggers/filters.py</code> <pre><code>def filter(self, record: logging.LogRecord) -&gt; bool:\n    \"\"\"Filter function.\"\"\"\n    return record.thread == self._main_thread_id\n</code></pre>"},{"location":"reference/#python_template.loggers.filters.InfoFilter","title":"InfoFilter","text":"<p>               Bases: <code>Filter</code></p> <p>This filter only shows log entries for INFO level.</p>"},{"location":"reference/#python_template.loggers.filters.InfoFilter.filter","title":"filter","text":"<pre><code>filter(record)\n</code></pre> <p>Filter function.</p> Source code in <code>src/python_template/loggers/filters.py</code> <pre><code>def filter(self, record: logging.LogRecord) -&gt; bool:\n    \"\"\"Filter function.\"\"\"\n    return record.levelno == logging.INFO\n</code></pre>"},{"location":"reference/#python_template.loggers.filters.RemoveColorFilter","title":"RemoveColorFilter","text":"<p>               Bases: <code>Filter</code></p> <p>Remove color filter.</p>"},{"location":"reference/#python_template.loggers.filters.RemoveColorFilter.filter","title":"filter","text":"<pre><code>filter(record)\n</code></pre> <p>Filter function.</p> Source code in <code>src/python_template/loggers/filters.py</code> <pre><code>def filter(self, record: logging.LogRecord) -&gt; bool:\n    \"\"\"Filter function.\"\"\"\n    is_module_installed(module_name=\"click\", throw_error=True)\n    import click\n\n    if record and record.msg and isinstance(record.msg, str):\n        record.msg = click.unstyle(record.msg)\n\n    return True\n</code></pre>"},{"location":"reference/#python_template.loggers.filters.SimpleThreadFilter","title":"SimpleThreadFilter","text":"<pre><code>SimpleThreadFilter(thread_name)\n</code></pre> <p>               Bases: <code>Filter</code></p> <p>This filter only shows log entries for specified thread name.</p> <p>Parameters:</p> Name Type Description Default <code>thread_name</code> <code>str</code> <p>Name of the thread that will be filtered.</p> required Source code in <code>src/python_template/loggers/filters.py</code> <pre><code>def __init__(self, thread_name: str):\n    self.thread_name = thread_name\n    super().__init__()\n</code></pre>"},{"location":"reference/#python_template.loggers.filters.SimpleThreadFilter.filter","title":"filter","text":"<pre><code>filter(record)\n</code></pre> <p>Filter function.</p> Source code in <code>src/python_template/loggers/filters.py</code> <pre><code>def filter(self, record: logging.LogRecord) -&gt; bool:\n    \"\"\"Filter function.\"\"\"\n    return record.threadName == self.thread_name\n</code></pre>"},{"location":"reference/#python_template.loggers.filters.ThreadFilter","title":"ThreadFilter","text":"<pre><code>ThreadFilter(thread_id=None, thread_name=None)\n</code></pre> <p>               Bases: <code>Filter</code></p> <p>Only accept log records from a specific thread or thread name.</p> <p>Parameters:</p> Name Type Description Default <code>thread_id</code> <code>Optional[int]</code> <p>Id of the thread that will be filtered.</p> <code>None</code> <code>thread_name</code> <code>Optional[str]</code> <p>Name of the thread that will be filtered.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>Occurs when <code>thread_id</code> and/or <code>thread_id</code> not given.</p> Source code in <code>src/python_template/loggers/filters.py</code> <pre><code>def __init__(\n    self, thread_id: Optional[int] = None, thread_name: Optional[str] = None\n) -&gt; None:\n    if thread_id is None and thread_name is None:\n        raise ValueError(\"Must specify either thread_id or thread_name\")\n\n    self._thread_id = thread_id\n    self._thread_name = thread_name\n</code></pre>"},{"location":"reference/#python_template.loggers.filters.ThreadFilter.filter","title":"filter","text":"<pre><code>filter(record)\n</code></pre> <p>Filter function.</p> Source code in <code>src/python_template/loggers/filters.py</code> <pre><code>def filter(self, record: logging.LogRecord) -&gt; bool:\n    \"\"\"Filter function.\"\"\"\n    if self._thread_id is not None and record.thread != self._thread_id:\n        return False\n\n    return not (\n        self._thread_name is not None and record.threadName != self._thread_name\n    )\n</code></pre>"},{"location":"reference/#python_template.loggers.formatters","title":"formatters","text":"<p>Formatters for the logger.</p>"},{"location":"reference/#python_template.loggers.formatters.ColoredFormatter","title":"ColoredFormatter","text":"<p>               Bases: <code>Formatter</code></p> <p>Formatter for colored logs.</p> <p>It uses ansi escape codes to color the logs in the terminal.</p>"},{"location":"reference/#python_template.loggers.formatters.ColoredFormatter.format","title":"format","text":"<pre><code>format(record)\n</code></pre> <p>Format the log record.</p> Source code in <code>src/python_template/loggers/formatters.py</code> <pre><code>def format(self, record: logging.LogRecord) -&gt; str:\n    \"\"\"Format the log record.\"\"\"\n    log_level = record.levelname\n\n    log_color = _TerminalLogColor[log_level].value\n    formatted_record = super().format(record)\n\n    return f\"\\033[{log_color}{formatted_record}\\033[0m\"\n</code></pre>"},{"location":"reference/#python_template.loggers.setup","title":"setup","text":"<p>Logger initialization methods.</p>"},{"location":"reference/#python_template.loggers.setup.setup_logging","title":"setup_logging","text":"<pre><code>setup_logging(logging_config=None, default_level=logging.INFO)\n</code></pre> <p>Set up the logger using default or custom configuration.</p> <p>Parameters:</p> Name Type Description Default <code>logging_config</code> <code>dict | None</code> <p>Custom logging configuration.</p> <code>None</code> <code>default_level</code> <code>int</code> <p>Default level of the logger.</p> <code>INFO</code> Source code in <code>src/python_template/loggers/setup.py</code> <pre><code>def setup_logging(\n    logging_config: dict | None = None,\n    default_level: int = logging.INFO,\n) -&gt; None:\n    \"\"\"Set up the logger using default or custom configuration.\n\n    Args:\n        logging_config: Custom logging configuration.\n        default_level: Default level of the logger.\n    \"\"\"\n    loaded_config = DEFAULT_LOGGER_CONFIG if logging_config is None else logging_config\n\n    try:\n        logging.config.dictConfig(loaded_config)\n    except Exception as e:\n        message = f\"Error when loading given logging configuration. Using default configs. Error: {e}\"\n        print(message)  # noqa: T201\n        logging.basicConfig(level=default_level)\n</code></pre>"},{"location":"reference/#python_template.placeholder","title":"placeholder","text":"<p>Placeholder file to provide several sample math calculations.</p> <p>This module allows the user to make mathematical calculations. Adapted from: https://realpython.com/python-project-documentation-with-mkdocs/</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; from python_template import placeholder\n&gt;&gt;&gt; placeholder.add(2, 4)\n6.0\n&gt;&gt;&gt; placeholder.multiply(2.0, 4.0)\n8.0\n&gt;&gt;&gt; from python_template.placeholder import divide\n&gt;&gt;&gt; divide(4.0, 2)\n2.0\n</code></pre> <p>The module contains the following functions:</p> <ul> <li><code>add(a, b)</code> - Returns the sum of two numbers.</li> <li><code>subtract(a, b)</code> - Returns the difference of two numbers.</li> <li><code>multiply(a, b)</code> - Returns the product of two numbers.</li> <li><code>divide(a, b)</code> - Returns the quotient of two numbers.</li> </ul>"},{"location":"reference/#python_template.placeholder.add","title":"add","text":"<pre><code>add(a, b)\n</code></pre> <p>Compute and return the sum of two numbers.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; add(4.0, 2.0)\n6.0\n&gt;&gt;&gt; add(4, 2)\n6.0\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>a</code> <code>float | int</code> <p>A number representing the first addend in the addition.</p> required <code>b</code> <code>float | int</code> <p>A number representing the second addend in the addition.</p> required <p>Returns:</p> Type Description <code>float</code> <p>A number representing the arithmetic sum result of <code>a</code> and <code>b</code>.</p> Source code in <code>src/python_template/placeholder.py</code> <pre><code>def add(a: float | int, b: float | int) -&gt; float:\n    \"\"\"Compute and return the sum of two numbers.\n\n    Examples:\n        &gt;&gt;&gt; add(4.0, 2.0)\n        6.0\n        &gt;&gt;&gt; add(4, 2)\n        6.0\n\n    Args:\n        a: A number representing the first addend in the addition.\n        b: A number representing the second addend in the addition.\n\n    Returns:\n        A number representing the arithmetic sum result of `a` and `b`.\n    \"\"\"\n    return float(a + b)\n</code></pre>"},{"location":"reference/#python_template.placeholder.divide","title":"divide","text":"<pre><code>divide(a, b)\n</code></pre> <p>Compute and return the division of two numbers.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; divide(4.0, 2.0)\n2.0\n&gt;&gt;&gt; divide(4, 2)\n2.0\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>a</code> <code>float | int</code> <p>A number representing the first divider in the divide.</p> required <code>b</code> <code>float | int</code> <p>A number representing the second divider in the divide.</p> required <p>Returns:</p> Type Description <code>float</code> <p>A number representing the division result of <code>a</code> and <code>b</code>.</p> <p>Raises:</p> Type Description <code>ZeroDivisionError</code> <p>If <code>b</code> is zero.</p> Source code in <code>src/python_template/placeholder.py</code> <pre><code>def divide(a: float | int, b: float | int) -&gt; float:\n    \"\"\"Compute and return the division of two numbers.\n\n    Examples:\n        &gt;&gt;&gt; divide(4.0, 2.0)\n        2.0\n        &gt;&gt;&gt; divide(4, 2)\n        2.0\n\n    Args:\n        a: A number representing the first divider in the divide.\n        b: A number representing the second divider in the divide.\n\n    Returns:\n        A number representing the division result of `a` and `b`.\n\n    Raises:\n        ZeroDivisionError: If `b` is zero.\n    \"\"\"\n    if b == 0:\n        raise ZeroDivisionError(\"division by zero\")\n\n    return float(a / b)\n</code></pre>"},{"location":"reference/#python_template.placeholder.multiply","title":"multiply","text":"<pre><code>multiply(a, b)\n</code></pre> <p>Compute and return the multiplication of two numbers.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; multiply(4.0, 2.0)\n8.0\n&gt;&gt;&gt; multiply(4, 2)\n8.0\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>a</code> <code>float | int</code> <p>A number representing the first multiplicator in the multiply.</p> required <code>b</code> <code>float | int</code> <p>A number representing the second multiplicator in the multiply.</p> required <p>Returns:</p> Type Description <code>float</code> <p>A number representing the multiplied result of <code>a</code> and <code>b</code>.</p> Source code in <code>src/python_template/placeholder.py</code> <pre><code>def multiply(a: float | int, b: float | int) -&gt; float:\n    \"\"\"Compute and return the multiplication of two numbers.\n\n    Examples:\n        &gt;&gt;&gt; multiply(4.0, 2.0)\n        8.0\n        &gt;&gt;&gt; multiply(4, 2)\n        8.0\n\n    Args:\n        a: A number representing the first multiplicator in the multiply.\n        b: A number representing the second multiplicator in the multiply.\n\n    Returns:\n        A number representing the multiplied result of `a` and `b`.\n    \"\"\"\n    return float(a * b)\n</code></pre>"},{"location":"reference/#python_template.placeholder.subtract","title":"subtract","text":"<pre><code>subtract(a, b)\n</code></pre> <p>Compute and return the substaction of two numbers.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; subtract(4.0, 2.0)\n2.0\n&gt;&gt;&gt; subtract(4, 2)\n2.0\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>a</code> <code>float | int</code> <p>A number representing the first substracter in the substract.</p> required <code>b</code> <code>float | int</code> <p>A number representing the second substracter in the substract.</p> required <p>Returns:</p> Type Description <code>float</code> <p>A number representing the substract result of <code>a</code> and <code>b</code>.</p> Source code in <code>src/python_template/placeholder.py</code> <pre><code>def subtract(a: float | int, b: float | int) -&gt; float:\n    \"\"\"Compute and return the substaction of two numbers.\n\n    Examples:\n        &gt;&gt;&gt; subtract(4.0, 2.0)\n        2.0\n        &gt;&gt;&gt; subtract(4, 2)\n        2.0\n\n    Args:\n        a: A number representing the first substracter in the substract.\n        b:  A number representing the second substracter in the substract.\n\n    Returns:\n        A number representing the substract result of `a` and `b`.\n    \"\"\"\n    return float(a - b)\n</code></pre>"},{"location":"reference/#python_template.scripts","title":"scripts","text":"<p>Useful python cli scripts.</p>"},{"location":"reference/#python_template.scripts.placeholder","title":"placeholder","text":"<p>Useful python cli scripts.</p> <p>Currently, <code>uv</code> not support this feature.</p>"},{"location":"reference/#python_template.scripts.placeholder.placeholder_script","title":"placeholder_script","text":"<pre><code>placeholder_script()\n</code></pre> <p>Script that can be run in the command line.</p> <p>More detail: https://setuptools.pypa.io/en/latest/userguide/entry_point.html</p> Source code in <code>src/python_template/scripts/placeholder.py</code> <pre><code>def placeholder_script():\n    \"\"\"Script that can be run in the command line.\n\n    More detail: https://setuptools.pypa.io/en/latest/userguide/entry_point.html\n    \"\"\"\n    print(\"This is a placeholder script that can be run as `entrypoint`\")  # noqa: T201\n</code></pre>"},{"location":"reference/#python_template.utils","title":"utils","text":"<p>Utility functions.</p>"},{"location":"reference/#python_template.utils.general","title":"general","text":"<p>General utility functions.</p>"},{"location":"reference/#python_template.utils.general.check_env_vars","title":"check_env_vars","text":"<pre><code>check_env_vars(env_vars=None)\n</code></pre> <p>Checks if the required environment variables are set.</p> <p>Parameters:</p> Name Type Description Default <code>env_vars</code> <code>list[str] | None</code> <p>List of environment variables to check. Defaults to None.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If any of the environment variables are not set.</p> Source code in <code>src/python_template/utils/general.py</code> <pre><code>def check_env_vars(env_vars: list[str] | None = None) -&gt; None:\n    \"\"\"Checks if the required environment variables are set.\n\n    Args:\n        env_vars: List of environment variables to check. Defaults to None.\n\n    Raises:\n        ValueError: If any of the environment variables are not set.\n    \"\"\"\n    if env_vars is None:\n        return\n\n    for env_var in env_vars:\n        if os.getenv(env_var) is None:\n            raise ValueError(f\"Please set {env_var} env var.\")\n</code></pre>"},{"location":"reference/#python_template.utils.general.is_module_installed","title":"is_module_installed","text":"<pre><code>is_module_installed(module_name, throw_error=False)\n</code></pre> <p>Check if the module is installed or not.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; is_module_installed(module_name=\"yaml\", throw_error=False)\nTrue\n&gt;&gt;&gt; is_module_installed(module_name=\"numpy\", throw_error=False)\nFalse\n&gt;&gt;&gt; is_module_installed(module_name=\"numpy\", throw_error=True)\nTraceback (most recent call last):\nImportError: Module numpy is not installed.\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>module_name</code> <code>str</code> <p>Name of the module to be checked.</p> required <code>throw_error</code> <code>bool</code> <p>If True, raises ImportError if module is not installed.</p> <code>False</code> <p>Returns:</p> Type Description <code>bool</code> <p>Returns True if module is installed, False otherwise.</p> <p>Raises:</p> Type Description <code>ImportError</code> <p>If throw_error is True and module is not installed.</p> Source code in <code>src/python_template/utils/general.py</code> <pre><code>def is_module_installed(module_name: str, throw_error: bool = False) -&gt; bool:\n    \"\"\"Check if the module is installed or not.\n\n    Examples:\n        &gt;&gt;&gt; is_module_installed(module_name=\"yaml\", throw_error=False)\n        True\n        &gt;&gt;&gt; is_module_installed(module_name=\"numpy\", throw_error=False)\n        False\n        &gt;&gt;&gt; is_module_installed(module_name=\"numpy\", throw_error=True)\n        Traceback (most recent call last):\n        ImportError: Module numpy is not installed.\n\n    Args:\n        module_name: Name of the module to be checked.\n        throw_error: If True, raises ImportError if module is not installed.\n\n    Returns:\n        Returns True if module is installed, False otherwise.\n\n    Raises:\n        ImportError: If throw_error is True and module is not installed.\n    \"\"\"\n    try:\n        importlib.import_module(module_name)\n        return True\n    except ImportError as e:\n        if throw_error:\n            message = f\"Module {module_name} is not installed.\"\n            raise ImportError(message) from e\n        return False\n</code></pre>"},{"location":"tutorials/","title":"Tutorials","text":"<p>This part of the project documentation focuses on a learning-oriented approach. You'll learn how to get started with the code in this project.</p> <p>Note: Expand this section by considering the following points:</p> <ul> <li>Help newcomers with getting started</li> <li>Teach readers about your library by making them     write code</li> <li>Inspire confidence through examples that work for     everyone, repeatably</li> <li>Give readers an immediate sense of achievement</li> <li>Show concrete examples, no abstractions</li> <li>Provide the minimum necessary explanation</li> <li>Avoid any distractions</li> </ul>"}]}